# 测试环境配置：最小化外部依赖，便于 CI/本地跑测
server.port=0
schema.file.path=./ontology/schema.yaml
schema.system.file.path=./ontology/schema-system.yaml
storage.type=file
data.root.path=./target/test-data
log.level=warn
# 不连接真实 DB/Neo4j（若测试需要可在此覆盖）
spring.autoconfigure.exclude=
